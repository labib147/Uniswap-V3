/// <reference types="node" />
import 'json-bigint-patch';
import { Server } from 'http';
import { MeshInstance, ServeMeshOptions } from '@graphql-mesh/runtime';
import { GraphQLMeshCLIParams } from '../..';
export declare function serveMesh({ baseDir, argsPort, getBuiltMesh, logger, rawConfig, playgroundTitle }: ServeMeshOptions, cliParams: GraphQLMeshCLIParams): Promise<{
    mesh: MeshInstance<any>;
    httpServer: Server;
    app: import("express-serve-static-core").Express;
    readyFlag: boolean;
    logger: import("../../../../types/src").Logger;
}>;
