import { RequestHandler } from 'express';
import { Logger } from '@graphql-mesh/types';
export declare const playgroundMiddlewareFactory: ({ baseDir, graphqlPath, logger, title, }: {
    baseDir: string;
    graphqlPath: string;
    logger: Logger;
    title?: string;
}) => RequestHandler;
