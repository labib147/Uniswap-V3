import { GraphQLSchema, DocumentNode } from 'graphql';
import { ExecuteMeshFn, GetMeshOptions, SubscribeMeshFn } from './types';
import { MeshPubSub, KeyValueCache, RawSourceOutput, Logger } from '@graphql-mesh/types';
import { envelop } from '@envelop/core';
declare type EnvelopPlugins = Parameters<typeof envelop>[0]['plugins'];
export interface MeshInstance<TMeshContext = any> {
    execute: ExecuteMeshFn;
    subscribe: SubscribeMeshFn;
    schema: GraphQLSchema;
    rawSources: RawSourceOutput[];
    destroy: () => void;
    pubsub: MeshPubSub;
    cache: KeyValueCache;
    logger: Logger;
    meshContext: TMeshContext;
    plugins: EnvelopPlugins;
    getEnveloped: ReturnType<typeof envelop>;
    sdkRequesterFactory: (globalContext: any) => (document: DocumentNode, variables?: any, operationContext?: any) => any;
}
export declare function getMesh<TMeshContext = any>(options: GetMeshOptions): Promise<MeshInstance<TMeshContext>>;
export {};
